# Week 01: Setup, Python, And PyTorch

## Goal

Run one complete PyTorch training loop and understand each moving part.

## Today Checklist

- [ ] Create `.venv` with Python 3.12.
- [ ] Install dependencies from `requirements.txt`.
- [ ] Run `labs/week-01-pytorch-basics/train_tiny_mlp.py`.
- [ ] Write down the tensor shapes.
- [ ] Explain `forward -> loss -> backward -> optimizer.step`.
- [ ] Create `checkpoints/week-01.md`.

## Commands

```bash
/opt/anaconda3/bin/python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python labs/week-01-pytorch-basics/train_tiny_mlp.py
```

## Shape Notes

- `x_train`:
- `y_train`:
- `prediction`:

## Concepts

### Tensor

Write this in your own words.

### `nn.Module`

Write this in your own words.

### Loss

Write this in your own words.

### `loss.backward()`

Write this in your own words.

### `optimizer.step()`

Write this in your own words.

## Questions

- 

